Ag Weather automated mailer for <%=@date.strftime('%m/%d/%Y') %>

The daily ET data for the site(s) you requested is shown below for <%= @date.strftime('%m/%d/%Y') %>.


Site Id                      Lat/Long  ET Value (in/day)
-------------------------  ----------  -----------------
<% @values.each do |sub| %>
<%= sprintf("%-25s  %.1f/%.1f  %.3f", sub[:site_name], sub[:latitude], sub[:longitude], sub[:values][0]) %><%= " (no data)" if sub[:values][0] < 0 %>
<% end %>


Each year the first ET email will arrive in your in-box on April 1st and the last on Sept 30th.

If you no longer wish to receive daily ET emails, please copy this url to your browser to be removed: <%= unsubscribe_subscriber_url(@subscriber, token: @subscriber.confirmation_token) %>
